//O null é um tipo especial, pois pode ser traduzido como “ausência de valor”
let input = null

if (input === null) {
    console.log('não há informação');
   } else {
    console.log(input);
   }

//undefined representa “ausência de valor”
let input2;
console.log(input2);
//enquanto null é um valor atribuído a uma variável que existe e foi iniciada, 
//undefined se refere ao valor de uma variável que não foi inicializada (ou seja,
//não foi atribuído nenhum valor a ela).

//normalmente undefined é atribuida a um erro no codigo por exemplo
